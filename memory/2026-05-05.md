# 2026-05-05

## Signal Engine skills packaged

Created proper local skill folders for the older slash-command workflows:

- `skills/signals/SKILL.md` for `/signals`
- `skills/radar/SKILL.md` for `/radar`
- `skills/bigpic/SKILL.md` for `/bigpic`
- `skills/ideas/SKILL.md` for `/ideas`

These skills point at the existing Signal Engine vault:

- Sources: `C:\Users\matth\Documents\openclaw-vault\signals\sources.md`
- Reports: `C:\Users\matth\Documents\openclaw-vault\signals\full`, `radar`, `bigpic`, `ideas`

Important rule: these are current-news workflows. When running them, fetch and
verify current sources instead of relying on old memory. Skip paywalled or dead
sources and list them at the bottom of the report.

For `/ideas`, business ideas should primarily tie to SmartCore Solutions unless
Matthew explicitly asks for Alert Pro, Bulldawg, or Teton.

## Arbitrage Deal Desk final handoff polish

Packaged the retro tech arbitrage workflow for Frankie/OpenClaw:

- `scripts/run_arbitrage_pipeline.py` is the safe one-command runner.
- Default command is local-only and does not spend Apify money:
  `python scripts\run_arbitrage_pipeline.py`
- Fresh paid collection requires explicit `--collect` and `APIFY_TOKEN`.
- `scripts/run_ebay_sold_comps.py` runs exact-model eBay sold comps, refuses
  broad keywords, sanitizes raw actor output, cleans comps, merges into
  `data/ebay_sold_comps_clean.csv`, and refreshes reports.
- `reports/OPENCLAW_ARBITRAGE_RUNBOOK.md` is the main handoff guide.
- `skills/arbitrage-deal-desk/SKILL.md` tells Frankie when and how to use the
  pipeline.

Important rule: no BUY verdict, no fake profit, no broad sold-comp searches,
and no paid actor run unless Matthew explicitly asks.

## Navigation and skill recall layer

Added a stronger front-door system so Frankie can find skills without Matthew
having to point at individual files:

- `START_HERE.md` is the main workspace map.
- `COMMANDS.md` maps `/signals`, `/radar`, `/bigpic`, `/ideas`, arbitrage,
  page-map, and trigger phrases to skill files.
- `skills/manifest.json` is the machine-readable skill index.
- `.openclaw/skill-manifest.json` is the OpenClaw-facing copy.
- `scripts/list_skills.py` prints the manifest in the terminal.

Startup rule added to `AGENTS.md`: when a skill or slash command is missing,
check `COMMANDS.md`, `skills/manifest.json`, `.openclaw/skill-manifest.json`,
and `skills/*/SKILL.md` before saying it cannot be found.

Also added project cards:

- `projects/frankie-mind.md`
- `projects/arbitrage-deal-desk.md`
- `projects/signal-engine.md`

## Workspace cleanup and health check

Added a final navigation/maintenance pass:

- `scripts/workspace_doctor.py` validates core navigation files, skill
  manifests, active Python scripts, `.gitignore` safety patterns, and
  token-shaped strings.
- `scripts/README.md`, `reports/README.md`, `docs/README.md`, and
  `data/README.md` now mark what is current vs. historical so Frankie does not
  wander through old experiments first.
- `START_HERE.md`, `COMMANDS.md`, and `TOOLS.md` now point to the doctor
  command.
- Old Apify debug docs/reports had token-shaped strings; those were redacted.
- `.gitignore` was tightened for raw browser tests, active-search JSON, local
  lead dumps, visual OCR output, screenshots, and test result JSON.

## Frankie pull-and-start handoff

Added a final handoff layer so Frankie has one obvious command after pulling
from GitHub:

```powershell
python scripts\frankie_start.py
```

This local-only startup command runs the workspace doctor, prints available
skills, runs the arbitrage pipeline dry run, and shows safe next commands. The
new `FRANKIE_HANDOFF.md` file explains the full startup path, skill routing,
arbitrage commands, sold-comp rules, and safety rules in one place.
