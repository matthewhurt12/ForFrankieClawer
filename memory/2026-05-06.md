# 2026-05-06

## Freebie Radar

Matthew wants a separate daily-style scan for Facebook Marketplace free,
curb-alert, pickup-only, and very cheap local tech/electronics listings around
Athens / Georgia. This should stay separate from the arbitrage Deal Desk.

Added:

- `scripts/freebie_radar.py`
- `config/freebie_radar.json`
- `skills/freebie-radar/SKILL.md`
- `docs/FREEBIE_RADAR.md`
- `reports/FREEBIE_RADAR_LAST.md`

Rules:

- Default command is local-only: `python scripts\freebie_radar.py`
- Paid fresh scan requires Matthew approval:
  `python scripts\freebie_radar.py --collect --sources facebook`
- `$1` listings are price bait unless title text clearly says free/pickup/curb
  alert.
- Seller messages are drafts only; do not automate contact.

## Athens Food Corrections

Matthew corrected the Athens restaurant recommender: when he says "no fast
food," that must also mean no fast casual, quick-service, or counter-service
places. Maepole should be rejected under that condition because Google describes
it as "Healthy Fast Food Near Me."

Added a safer final-answer path:

- `python scripts\athens_food_go_now.py --mood "lunch hot fresh" --no-fast-food --no-bar-food`

This ranks candidates locally, then uses the Apify Google search actor as a
last-mile gate for open/closed status and condition mismatches before Frankie
presents a "go now" pick.

## Athens Restaurant List Refresh

Matthew asked to expand and update the Athens restaurant list before building
more on the food recommender.

Added:

- `scripts\refresh_athens_restaurants.py`
- `reports\ATHENS_RESTAURANT_REFRESH_001.md`

Refresh result:

- `data\restaurants\athens_restaurants.csv` now has 448 rows.
- 330 rows are `Directory Import` discovery candidates from OpenStreetMap /
  Overpass and Visit Athens.
- Imported rows are lower confidence and should not be treated as Matthew-rated
  favorites.
- Closed/inactive rows now include Birdies, The Grit, home.made, and Weaver D's.
- Specific `--query` searches now behave like filters so imported categories
  such as Korean can surface correctly.

Repeat command:

```powershell
python scripts\refresh_athens_restaurants.py
```
