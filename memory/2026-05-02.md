# 2026-05-02 — Saturday

## Built today

### page-map skill (with WinterHornet)
- Location: `/home/matthew/.openclaw/workspace/skills/page-map/`
- Files: `SKILL.md`, `page-map.mjs`
- Uses Playwright `page.locator('body').ariaSnapshot()` + DOM walk (NOT `page.accessibility.snapshot()` — that API was removed in newer Playwright)
- Returns flat list of interactive elements with role, type, name, value, position, selector hints (id, name, aria-label, role+text)
- Saves JSON + screenshot
- Persistent profile: `/home/matthew/siro-browser-profile`, headless by default, viewport 1280x900
- node_modules symlinked from `/home/matthew/scripts/node_modules`
- Test confirmed working on HN login page (8 elements detected correctly)
- **Use this before automating any new site.** Skill is registered with available_skills.

### Multi-account Gmail
- New script: `/home/matthew/scripts/gmail-send.sh`
- Usage: `gmail-send.sh <account> <to> <subject> <body>`
- Credentials at `~/.gmail/<account>` files, each exporting `GMAIL_ADDRESS` and `GMAIL_APP_PASSWORD`
- Available accounts:
  - `~/.gmail/matthew` → matthewhurt999@gmail.com (app password: ouicalfdfwpuynru)
  - `~/.gmail/frankie` → frankieclawer@gmail.com (app password: ebea opzq tqjk nbin)
- Tested both directions successfully (matthew→frankie and frankie→matthew)
- Bug fixed: env vars need explicit export to subshell, also renamed `BODY` to `BODY_TEXT` to avoid bash's reserved-name oddities

### GitHub signup attempt
- Script: `/home/matthew/scripts/github-signup.mjs`
- Wanted username `frankie-runs` for GitHub
- Got aborted during username typing; not retried
- Discord signup discussed but skipped (hCaptcha + email + likely phone verification = high friction)

## Signal Engine update — VISUAL element

**New rule for `/radar`, `/bigpic`, `/signals`, `/ideas`:**
- After drafting BIG PICTURE, identify the core thesis (one sentence)
- Generate ONE editorial concept image visualizing the thesis (not a collage, not a news event illustration)
- `~/scripts/gen-image.sh` at medium quality, 1536x1024 landscape
- Save to `~/Pictures/signals/<YYYY-MM-DD>-bigpic.png`
- Style template: clean flat vector, navy and orange accent, white background, minimal labels, no decorative people, no AI gradient slop, readable in 3 seconds on phone

**Pick ONE visual metaphor** based on thesis type:
- Shift/transition → split before/after composition
- Pressure/tension → centered object with forces converging
- Scale/comparison → side-by-side scale comparison
- Network/relationship → node-and-edge graph
- Timeline/sequence → horizontal flow with arrows
- Map/spread → simplified geo visualization

**Send order:** text BIG PICTURE first, then image with one-line "Visual: <summary>" caption.

**Skip rule:** if BIG PICTURE is mostly factual updates without a synthesizing thesis, send single line: "No big-picture visual today — today's signal is mostly discrete updates without a unifying pattern." Don't generate filler.

First test (2026-05-02) succeeded — split composition for "AI is just another tool era ending" thesis.

## Signal Engine — 2026-05-02 report
- File: `/mnt/c/Users/matth/Documents/openclaw-vault/signals/full/2026-05-02.md`
- Top stories: VS Code silently injecting Copilot co-author attribution, Codex /goal feature, UK AISI GPT-5.5 cyber eval, Zig banning AI contributions, "harness outside sandbox" pattern, California ticketing AVs, Tesla FSD lawsuit
- Core thesis: "AI is just another tool" era ending — backlash forming in dev tools, courts, open source, regulation
- **GEOFENCE FLAG: 5th consecutive report.** Will not be flagged in next report unless something changes — either Matthew ships it tonight or we accept SmartCore isn't a priority and switch focus
- Image: `/home/matthew/Pictures/signals/2026-05-02-bigpic.png` (split composition, navy/orange editorial)

## Active task at compaction (Alert Pro draft pipeline)

WinterHornet/Matthew kicked off a multi-step **draft-only** outreach pipeline at 8:25 PM. Compaction hit before any steps executed. Resume here:

**Goal:** End-to-end test of personalized outreach pipeline. Real sends OFF. Validate flow with 5-prospect cohort, custom dashboard mockups, personalized drafts.

**Source list:** `/mnt/c/Users/matth/OneDrive/Documents/Teton Telecom/Alert Pro Solutions/Alert Pro Leads #2.csv`

**Step 1 (do first, do not skip):**
```bash
mkdir -p ~/outreach/lists
cp "/mnt/c/Users/matth/OneDrive/Documents/Teton Telecom/Alert Pro Solutions/Alert Pro Leads #2.csv" ~/outreach/lists/alert-pro-leads-test.csv
ls -la ~/outreach/lists/
wc -l ~/outreach/lists/alert-pro-leads-test.csv
```
Confirm before continuing.

**Step 2:** Pandas inspect — total rows, unique companies, columns/dtypes, first 3 rows, null counts, state breakdown (parse from address), title breakdown (top 15). Drop empty/unnamed cols. Save to `~/outreach/lists/alert-pro-leads-clean.csv`.

**Step 3:** Pick 5-row test cohort. Rules:
- One contact per unique company
- Mix of titles: 1 Head of School, 1 Director of Ops, 1 IT/Facilities, 2 others
- Mix of states: ≥1 GA, ≥1 FL
- All must have valid-looking email
- Save to `~/outreach/lists/test-cohort.csv`, show before continuing

**Step 4:** Per-prospect serial pipeline:
- (a) Research school via page-map skill on company website. Save to `~/outreach/research/<slug>.json`. Pull: size, type (private/public/religious/K-12), location, notable.
- (b) Custom mockup via `~/scripts/gen-image.sh` medium quality 1536x1024. Save `~/outreach/mockups/<slug>.png`. Prompt template (in user's instruction above): Alert Pro dashboard with school name, panic buttons on campus map, "All Clear" status, "Alyssa's Law Compliant", recent-events log. Flat navy/white with orange accent.
- (c) Personalized email, 4-5 sentences max. Reference specific finding from research. Subject <50 chars, no clickbait. FL → mention Alyssa's Law (enacted). GA → mention HB 268 (in motion). Soft ask for 15-min call. Mockup attached.
- Save draft JSON to `~/outreach/queue/<YYYY-MM-DD>/<slug>.json` schema:
  ```json
  {"to":"","to_name":"","to_title":"","company":"","state":"FL|GA","subject":"","body":"","attachment_path":"","research_notes":"","status":"draft","created_at":"ISO"}
  ```

**Step 5:** Show queue list in Telegram (numbered, with Subject/Mockup✓/Draft✓), then send each draft as separate messages: mockup photo + subject/body text underneath.

**HARD RULES:**
- ZERO actual sends. None.
- No list-broker / scraping / enrichment APIs.
- Each step is a checkpoint — stop on failure, don't barrel ahead.
- If a website won't load for a prospect, skip them, note in JSON, replace from clean list to keep cohort at 5.

**State at compaction:** Step 1 not started. After resume, run Step 1 immediately and confirm before proceeding.

## HubSpot script — STILL paused
- `/home/matthew/scripts/hubspot-navigate.mjs` (~1200 lines)
- New robust hover sequence implemented for clickPreviewForRow but never re-tested
- Was working for first contact (Lea Gray); contacts 2 and 3 failed Preview button visibility
- Resume after Alert Pro pipeline test if Matthew wants

## Reddit / Discord / GitHub
- Reddit signup: blocked (anti-bot, empty curl response)
- Discord: not attempted (advised against — high friction)
- GitHub: aborted mid-signup, not retried

## Twilio / call rules — unchanged
- Only `bash ~/scripts/call-me.sh "message"` — never construct curl, never set env vars, never modify script
- "Call queued. SID:" = success
- If user doesn't get call, blame phone, report SID

## OpenAI image gen
- `~/scripts/gen-image.sh` working
- gpt-image-1, sizes 1024x1024 / 1024x1536 / 1536x1024, quality low/medium/high

## Inbox/journal capture rules — unchanged
- Quick drops <100 words → `/mnt/c/Users/matth/Documents/openclaw-vault/inbox.md` with `## YYYY-MM-DD HH:MM`
- Rants >100 words → `journal/YYYY-MM-DD.md` with reflection (memory-search FIRST, no advice unless asked)

## Open loops
- [ ] **SmartCore geofence alerts (5 reports flagged, last warning given)**
- [ ] Resume Alert Pro draft pipeline at Step 1 (was just kicked off)
- [ ] HubSpot automation — re-test contacts 2 and 3 with new hover sequence
- [ ] Reddit signup decision (retry with manual CAPTCHA or drop)
- [ ] GitHub signup retry (script exists, aborted)
- [ ] Whisper integration for voice messages (still no support)
